import assert from 'node:assert/strict';
import test from 'node:test';
import { createApp } from './createApp';
import { registerAuthRoute } from './auth-route';
import type { SessionIssuer } from '../auth/session';
import type { SqlExecutor } from '../auth/postgres-repository';
const dbFixture=(scope={organization_id:'org-1',store_id:'store-1',device_id:'device-1'}):SqlExecutor=>({async query<T extends Record<string,unknown>>(sql:string):Promise<readonly T[]>{if(sql.includes('FROM prodx_organizations'))return[scope as unknown as T];if(sql.includes('FROM prodx_store_memberships'))return[{allowed:true} as unknown as T];if(sql.includes('FROM prodx_users'))return[{user_id:'user-1'} as unknown as T];return[]}});
const requestJson=async(app:ReturnType<typeof createApp>,path:string,body?:unknown,headers?:Record<string,string>)=>{const server=await new Promise<import('node:http').Server>(resolve=>{const instance=app.listen(0,()=>resolve(instance));});try{const address=server.address();if(!address||typeof address==='string')throw new Error('Test server did not expose a port.');return await fetch(`http://127.0.0.1:${address.port}${path}`,{method:body===undefined?'GET':'POST',headers:{...(body===undefined?{}:{'content-type':'application/json'}),...headers},...(body===undefined?{}:{body:JSON.stringify(body)})});}finally{await new Promise<void>((resolve,reject)=>server.close(error=>error?reject(error):resolve()));}};
const loginBody={organizationSlug:'org-1',storeCode:'store-1',emailOrPin:'cashier',passwordOrPin:'secret',registerId:'REG-01'};
const issuer=(overrides:Partial<SessionIssuer>={}):SessionIssuer=>({authenticateCredentials:async()=>({token:'token',sessionId:'session',expiresAt:new Date('2026-09-13T20:00:00.000Z')}),authenticateBearer:async()=>null,revokeBearer:async()=>true,...overrides});
test('login is explicitly public and returns a tenant/store/register-bound session',async()=>{const app=createApp({authenticateRequest:async()=>null,publicPaths:['/api/v1/auth/login'],configureRoutes:a=>registerAuthRoute(a,issuer(),dbFixture())});const response=await requestJson(app,'/api/v1/auth/login',loginBody);assert.equal(response.status,200);assert.deepEqual(await response.json(),{token:'token',sessionId:'session',expiresAt:'2026-09-13T20:00:00.000Z',organizationId:'org-1',storeId:'store-1',userId:'user-1',registerId:'REG-01'});});
test('invalid login shape is rejected before authentication',async()=>{let called=false;const app=createApp({authenticateRequest:async()=>null,publicPaths:['/api/v1/auth/login'],configureRoutes:a=>registerAuthRoute(a,issuer({authenticateCredentials:async()=>{called=true;return null}}),dbFixture())});const response=await requestJson(app,'/api/v1/auth/login',{username:'cashier',password:'secret',deviceId:'device'});assert.equal(response.status,400);assert.equal(called,false);});
test('failed login does not disclose credential details',async()=>{const app=createApp({authenticateRequest:async()=>null,publicPaths:['/api/v1/auth/login'],configureRoutes:a=>registerAuthRoute(a,issuer({authenticateCredentials:async()=>null}),dbFixture())});const response=await requestJson(app,'/api/v1/auth/login',loginBody);assert.equal(response.status,401);assert.equal((await response.json()).error.code,'INVALID_CREDENTIALS');});
test('login cannot issue a session for a user outside the resolved store',async()=>{let calls=0;const db:SqlExecutor={async query<T extends Record<string,unknown>>(sql:string):Promise<readonly T[]>{if(sql.includes('FROM prodx_organizations'))return[{organization_id:'org-1',store_id:'store-1',device_id:'device-1'} as unknown as T];if(sql.includes('FROM prodx_users'))return[{user_id:'user-1'} as unknown as T];if(sql.includes('FROM prodx_store_memberships'))return[{allowed:false} as unknown as T];return[]}};const app=createApp({authenticateRequest:async()=>null,publicPaths:['/api/v1/auth/login'],configureRoutes:a=>registerAuthRoute(a,issuer({authenticateCredentials:async()=>{calls+=1;return null}}),db)});const response=await requestJson(app,'/api/v1/auth/login',loginBody);assert.equal(response.status,401);assert.equal(calls,0);});
test('authenticated session endpoint rejects missing bearer token',async()=>{const app=createApp({authenticateRequest:async()=>null,configureRoutes:a=>registerAuthRoute(a,issuer(),dbFixture())});const response=await requestJson(app,'/api/v1/auth/session');assert.equal(response.status,401);});
test('logout revokes the supplied bearer session',async()=>{let token='';const app=createApp({authenticateRequest:async()=>({userId:'user-1',organizationId:'org-1',storeId:'store-1'}),configureRoutes:a=>registerAuthRoute(a,issuer({revokeBearer:async(value)=>{token=value;return true}}),dbFixture())});const response=await requestJson(app,'/api/v1/auth/logout',undefined,{authorization:'Bearer secret-token'});assert.equal(response.status,204);assert.equal(token,'secret-token');});
